<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "Pe Portfolio" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
  </head>

  <body class="min-h-screen bg-rose-50 text-slate-900">

    <!-- Header -->
    <header class="fixed top-0 left-0 right-0 z-50 bg-rose-50/90 backdrop-blur border-b border-rose-100">
      <div class="mx-auto max-w-5xl px-6 h-16 flex items-center justify-between">
        <%= link_to "PORTFOLIO.", root_path, class: "font-extrabold" %>

        <nav class="flex gap-2">
          <%= link_to "TOP", root_path, class: "pill" %>
          <%= link_to "ABOUT", about_path, class: "pill" %>
          <%= link_to "application", apps_path, class: "pill" %>
          <%= link_to "記事", articles_path, class: "pill" %>
          <%= link_to "学習", learning_path, class: "pill" %>
        </nav>
      </div>
    </header>

    <!-- Main -->
    <main class="pt-16 pb-24">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="fixed bottom-0 left-0 right-0 z-50 bg-white/90 backdrop-blur border-t border-slate-200">
      <div class="mx-auto max-w-5xl px-6 h-16 flex items-center justify-between">
        <div class="flex gap-4 text-sm">
          <a href="#">Github</a>
          <a href="#">X</a>
          <a href="#">問い合わせ</a>
        </div>

        <button id="themeToggle" class="pill bg-sky-200">Dark Mode</button>
      </div>
    </footer>

  </body>
</html>
