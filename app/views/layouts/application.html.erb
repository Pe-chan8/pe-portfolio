<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Pe Portfolio" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Pe Portfolio">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Tailwind build output (app/assets/builds/application.css) %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%# JS build output (app/assets/builds/application.js) %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="min-h-dvh bg-rose-50 text-slate-900 dark:bg-slate-950 dark:text-slate-100">
    <div class="min-h-dvh flex flex-col">
      <%# ===== Fixed Header ===== %>
      <header class="fixed top-0 left-0 right-0 z-50">
        <div class="mx-auto w-full max-w-5xl px-4 pt-4">
          <div class="card px-4 py-3">
            <div class="flex flex-wrap items-center justify-between gap-3">
              <nav class="flex flex-wrap gap-2">
                <%= link_to "TOP", root_path, class: "pill" %>
                <%= link_to "About", about_path, class: "pill" %>
                <%= link_to "App", apps_path, class: "pill" %>
                <%= link_to "記事", articles_path, class: "pill" %>
                <%= link_to "Books", books_path, class: "pill" %>
                <%= link_to "問合せ", contact_path, class: "pill" %>
              </nav>

              <div data-controller="dark-mode">
                <button class="btn" data-action="click->dark-mode#toggle">
                  <span data-dark-mode-target="label">Dark Mode</span>
                </button>
              </div>
            </div>
          </div>
        </div>
      </header>

      <%# ===== Main (space for fixed header/footer) ===== %>
      <main class="flex-1 pt-24 pb-28">
        <div class="mx-auto w-full max-w-5xl px-4">
          <%= yield %>
        </div>
      </main>

      <%# ===== Fixed Footer ===== %>
      <footer class="fixed bottom-0 left-0 right-0 z-40">
        <div class="mx-auto w-full max-w-5xl px-4 pb-4">
          <div class="card px-6 py-4">
            <div class="flex flex-wrap items-center justify-end gap-6 text-sm">
              <a class="hover:underline" href="#" target="_blank" rel="noreferrer">Github</a>
              <a class="hover:underline" href="#" target="_blank" rel="noreferrer">X</a>
              <a class="hover:underline" href="#" target="_blank" rel="noreferrer">note</a>
              <a class="hover:underline" href="#" target="_blank" rel="noreferrer">Qiita</a>
            </div>
          </div>
        </div>
      </footer>
    </div>
  </body>
</html>
